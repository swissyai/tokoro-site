{
  "name": "tokoro-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "npm run check && npx --yes wrangler@4.125.0 deploy",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check script.js && node --check build.mjs && node --test tests/*.test.mjs && node build.mjs"
  }
}
